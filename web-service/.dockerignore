# Specifies files which should be ignored by docker. This means in effect that
# the ignored files are not sent to the docker build server, and building is
# faster because only required files are transfered.

code-coverage/
config/
src/
vendor/

.*ignore
composer.*
Dockerfile.*
Makefile
phpstan.neon
phpunit.xml*
psalm.xml
README.md
